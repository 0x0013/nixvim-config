{
  plugins.treesitter = {
    enable = true;
    folding = true;

    settings = {
      highlight = {
        enable = true;
      };

      indent.enable = true;
      playground.enable = true;
    };
  };
}
